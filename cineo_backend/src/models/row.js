class Row {
    constructor(rowId, roomId, seatCount, rowNumber) {
        this.rowId = rowId;
        this.roomId = roomId;
        this.seatCount = seatCount;
        this.rowNumber = rowNumber;
    }
}

module.exports = Row;
